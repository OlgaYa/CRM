<div id="user_nav">
  <% if user_signed_in? %>
    Signed in as <%= current_user.email %>. Not you?
    <%= link_to "Sign out", destroy_user_session_path, :method => :delete %>
    <% if user_admin? %>
    	<%= link_to "Add new user",  new_admin_registration_path, :method => :get %>
    <% end %>	
  <% else %>
    <%= link_to "sign in", new_user_session_path %>
  <% end %>
</div>

