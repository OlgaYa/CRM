<%= content_for(:reports) { 'open' } %>

<ul id='tables-sub-menu-nav' class="nav navbar-nav">
  <%= sub_menu_action('Summary reports', summary_reports_path, 'summary_reports', 'index') if can? :index, :summary_report %>
  <%= sub_menu_action('All reports', '#', '', '') if can? :all_reports, :reports %>
  <%= sub_menu_action('My reports', reports_path, 'reports', 'index') if can? :index, Report %>
</ul>
